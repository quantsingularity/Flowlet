apiVersion: v1
kind: Service
metadata:
  name: flowlet-frontend
  namespace: flowlet-core
  labels:
    app: flowlet
    tier: frontend
spec:
  selector:
    app: flowlet
    tier: frontend
  ports:
    - name: http
      port: 80
      targetPort: 3000
      protocol: TCP
  type: ClusterIP
